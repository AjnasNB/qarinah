{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://qarinah.io/schemas/federated-context.schema.json",
  "title": "Qarinah federated context",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "query", "authorityBoundary", "workspaces", "repositoryGraph", "manifestHash"],
  "properties": {
    "schemaVersion": { "const": "qarinah.federated-context.v1" },
    "query": { "type": "string", "maxLength": 4096 },
    "authorityBoundary": { "const": "separate-packs" },
    "workspaces": {
      "type": "array",
      "minItems": 1,
      "maxItems": 32,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["authority", "repositoryId", "workspaceId", "pack"],
        "properties": {
          "authority": { "type": "string", "minLength": 1, "maxLength": 256 },
          "repositoryId": { "type": "string", "minLength": 1, "maxLength": 256 },
          "workspaceId": { "type": "string" },
          "pack": { "type": "object" }
        }
      }
    },
    "repositoryGraph": {
      "type": "array",
      "maxItems": 256,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["from", "to", "type"],
        "properties": {
          "from": { "type": "string", "minLength": 1, "maxLength": 256 },
          "to": { "type": "string", "minLength": 1, "maxLength": 256 },
          "type": { "enum": ["depends_on", "documents", "deploys", "shares_contract", "owned_by", "references"] }
        }
      }
    },
    "manifestHash": { "type": "string", "pattern": "^sha256:[a-f0-9]{64}$" }
  }
}
